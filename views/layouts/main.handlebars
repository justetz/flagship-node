<!DOCTYPE html>
<html>
<head>
    <title>{{pageTitle}} | Flagship Docs</title>
    <link href="/stylesheets/bootswatch-flatly.min.css" rel="stylesheet" type="text/css">
    <link href="/stylesheets/main.css" rel="stylesheet" type="text/css" />
    <meta charset="utf-8" />
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="#">Flagship Docs</a>
      </div>

      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li><a href="#">Sign in</a></li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container-fluid main full-height">
    <div class="row full-height">
      <div class="col-md-4 col-lg-2 sidebar">
        <a class="btn btn-primary btn-upload" href="/upload">Test</a>
        {{#each categories}}
        <a href="#">{{name}}</a>
        {{/each}}
      </div>
      <div class="col-lg-10">
        <h1 class="heading">{{pageTitle}}</h1>
        {{{body}}}
      </div>
    </div>
  </div>
</body>
</html>
